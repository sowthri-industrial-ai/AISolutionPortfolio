"""Fruit-market demo vertical for HelloAgenticAI.

This package is the v1 demo layer that exercises the framework end-to-end:
six mock shop tools, a FruitMarketAgent (subclass of AgentBase), and a
Chainlit UI that streams the live agent loop.

Refinery and other future verticals follow the same shape — swap shops
for domain-specific tools, swap prompts for domain-specific personas,
keep the framework intact.
"""
